/**
 * 앱 전체 설정값 — 조정할 숫자는 전부 이 파일에만 있습니다.
 * (화면 크기·색상 같은 시각 요소는 css/style.css 맨 위 :root 변수에 모아두었습니다.)
 */

export const CONFIG = {
  /* ── 음성 (TTS) ────────────────────────────────── */
  speech: {
    lang: 'ko-KR',
    // 나긋나긋하게 들리도록: 조금 느리게 + 조금 낮은 톤.
    // (기획서 값은 rate 0.85였고, 설정 화면에서 부모가 속도·톤을 바꿀 수 있습니다)
    rate: 0.78,
    pitch: 0.92,
    volume: 1.0,

    // 부모가 고르지 않았을 때 이 순서대로 부드러운 목소리를 먼저 찾습니다.
    // 기기에 없는 이름은 그냥 건너뜁니다. (아이폰·맥 '유나', 안드로이드 구글 음성, 윈도우 'Heami')
    preferredVoices: [
      '유나', 'Yuna',
      'Google 한국의', '한국어 여성', 'Heami',
      'Sora', 'Shelley', 'Sandy', 'Flo',
    ],
    // 설정 화면의 말하기 속도 선택지
    rateOptions: [
      { key: 'slow', label: '천천히', rate: 0.68 },
      { key: 'normal', label: '보통', rate: 0.78 },
      { key: 'fast', label: '조금 빠르게', rate: 0.90 },
    ],
    // 설정 화면의 목소리 톤 선택지 (낮을수록 차분하고 나긋나긋합니다)
    pitchOptions: [
      { key: 'low', label: '낮게', pitch: 0.82 },
      { key: 'normal', label: '보통', pitch: 0.92 },
      { key: 'high', label: '높게', pitch: 1.05 },
    ],
    sampleText: '하나님이 세상을 이처럼 사랑하사',
    // 브라우저가 onend 이벤트를 안 주고 멈추는 경우를 대비한 안전장치.
    // 예상 재생시간(글자수 × charMs + padMs)의 2배가 지나면 강제로 다음 단계 진행.
    // 실측(맥 크롬, 유나 음성, rate 0.85): 17글자 = 2.75초 ≒ 글자당 162ms
    charMs: 140,
    padMs: 1200,
  },

  /* ── 타이밍 (밀리초) ───────────────────────────── */
  timing: {
    lineGapMs: 400,            // 낭독할 때 소절과 소절 사이 쉬는 시간
    introGapMs: 500,           // "○월 말씀" 안내 후 본문 시작까지
    replayGapMs: 600,          // 낭독 끝나고 다시 듣기 버튼이 살아나기까지

    // 따라 읽기
    micDelayMs: 500,           // 재생 직후 마이크 ON까지 대기 (앱 자기 목소리 오인 방지)
    micVoiceHoldMs: 1000,      // 이 시간 이상 소리가 지속되면 "따라 했다"고 인정
    micSilenceTimeoutMs: 8000, // 무음이 이 시간 지나면 조용히 다음 소절로
    autoAdvanceMs: 3000,       // 마이크 권한 없을 때: 재생 후 이만큼 기다렸다 다음 소절
    micPermissionTimeoutMs: 6000, // 권한 창을 안 누르고 두어도 이 시간 뒤엔 자동 진행으로 시작
    praiseGapMs: 300,          // 칭찬 후 다음 소절까지
    roundGapMs: 1200,          // 한 바퀴 끝내고 다시 1소절로 돌아가기까지

    // 퀴즈
    rewardMs: 1500,            // 정답 보상 연출 시간 → 자동으로 다음 문제
    wrongFeedbackMs: 900,      // 오답 피드백 시간 (보상의 절반 이하 강도)
    monthCompleteMs: 3500,     // 한 달 완주 시 도감 수집 연출 시간
    questionGapMs: 300,        // 문제와 문제 사이

    tapLockMs: 250,            // 유아 연타 방지
  },

  /* ── 마이크 ────────────────────────────────────── */
  mic: {
    // 이 볼륨(RMS 0~1)을 넘으면 '소리가 난다'고 봅니다.
    // 실측 기준값: 또렷한 말소리 0.15 · 작게 소곤거림 0.03 · 방 안 잔잔한 잡음 0.005
    // 올리면 둔해지고(아이가 작게 말하면 못 알아들음), 내리면 예민해집니다(TV·에어컨에도 넘어감).
    rmsThreshold: 0.02,
    silenceToleranceMs: 350,   // 말하다 잠깐 끊겨도 이 시간까지는 이어진 것으로 봄
  },

  /* ── 퀴즈 규칙 ─────────────────────────────────── */
  quiz: {
    choiceCount: 3,
    randomSetSize: 10,             // 랜덤 퀴즈 한 세트 문항 수
    randomCurrentMonthRatio: 0.5,  // 그중 이번 달 문제 비율
  },

  /* ── 음성 문구 ─────────────────────────────────── */
  // {name} 자리에 아이 이름이 들어갑니다. 이름을 입력하지 않았으면 그 문장은 건너뜁니다.
  praise: [
    '잘했어요!',
    '우와, 맞았어요!',
    '최고예요!',
    '딩동댕!',
    '정말 잘하는구나!',
    '우리 {name} 대단해요!',
  ],
  // 따라 읽기에서 아이 목소리가 들렸을 때 (짧게)
  repeatPraise: ['잘했어요!', '좋아요!', '아주 좋아요!'],
  // 오답일 때 (연출 없이 부드럽게 한 번만)
  wrongVoice: ['다시 한번 골라 볼까요?', '괜찮아요, 다시 골라 봐요.'],

  /* ── 저장소 키 ─────────────────────────────────── */
  storageKey: 'bible-memory-v1',
};

/** 개발 편의: 브라우저 콘솔에서 window.CONFIG 로 확인 가능 */
if (typeof window !== 'undefined') window.CONFIG = CONFIG;
